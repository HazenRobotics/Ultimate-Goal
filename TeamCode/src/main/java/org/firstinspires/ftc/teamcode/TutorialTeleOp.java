package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

// this is so that this class will display in the Driver Station app so that we can run it
@TeleOp(name = "TutorialTeleOp", group = "teleop")
public class TutorialTeleOp extends OpMode {
    
    
    @Override
    public void init( ) { // this will get any devices (i.e. motors, sensors, gyros, etc. )
    
    
    }
    
    @Override
    public void loop( ) { // this method will continuously loop while the program is running or the OpMode is active
    
    
    }
    
    
}